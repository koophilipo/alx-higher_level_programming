#!/usr/bin/python3

def add_tuple(tuple_a=(), tuple_b=()):
    a = 0
    b = 0
    if tuple_a and tuple_b:
        if len(tuple_a) > 0 or len(tuple_b) > 0:
            for i in range(1):
    new_tuple = (a, b)
